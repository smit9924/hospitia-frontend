import { CustomMatTooltip } from './custom-mat-tooltip';

describe('CustomMatTooltip', () => {
  it('should create an instance', () => {
    const directive = new CustomMatTooltip();
    expect(directive).toBeTruthy();
  });
});
