import { PasswordConfirmation } from './password-confirmation';

describe('PasswordConfirmation', () => {
  it('should create an instance', () => {
    const directive = new PasswordConfirmation();
    expect(directive).toBeTruthy();
  });
});
