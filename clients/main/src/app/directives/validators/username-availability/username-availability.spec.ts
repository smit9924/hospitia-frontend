import { UsernameAvailability } from './username-availability';

describe('UsernameAvailability', () => {
  it('should create an instance', () => {
    const directive = new UsernameAvailability();
    expect(directive).toBeTruthy();
  });
});
